meta {
  name: Crear
  type: http
  seq: 4
}

post {
  url: {{BACKEND_URL}}/api/admin/categories
  body: json
  auth: inherit
}

headers {
  Accept: application/json
  Content-Type: application/json
  Authorization: bearer {{TOKEN_STAFF}}
}

body:json {
  {
    "title": "Bebidas"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
