meta {
  name: Editar Cliente
  type: http
  seq: 3
}

put {
  url: {{BACKEND_URL}}/api/admin/clientes/2
  body: json
  auth: inherit
}

headers {
  Accept: application/json
  Content-Type: application/json
  Authorization: bearer {{TOKEN_STAFF}}
}

body:json {
  {
      "nombres": "Joel Daniel",
      "apellidos": "Cedeño Pin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
